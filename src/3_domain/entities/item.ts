export type Item = {
  marketHashName: string;
  currency: string;
  suggestedPrice: number;
  itemPage: string;
  marketPage: string;
  minPrice: number;
  maxPrice: number;
  meanPrice: number;
  medianPrice: number;
  quantity: number;
  createdAt: Date;
  updatedAt: Date;
};
